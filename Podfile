# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CHUnsplash' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CHUnsplash
  pod 'Toast-Swift', '~> 5.0.1'
  pod 'ProgressHUD'

  target 'CHUnsplashTests' do
    inherit! :search_paths
    # Pods for testing
  
  end

  target 'CHUnsplashUITests' do
    # Pods for testing
  end

end
